import React from "react";
import NavigationItems from "./NavigationItems";
const Toolbar = (props) => {
  return (
    <header>
      <div></div>
      <nav>
        <NavigationItems />
      </nav>
    </header>
  );
};

export default Toolbar;
